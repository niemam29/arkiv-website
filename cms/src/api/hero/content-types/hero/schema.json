{
  "kind": "singleType",
  "collectionName": "heroes",
  "info": {
    "singularName": "hero",
    "pluralName": "heroes",
    "displayName": "Hero Section"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "text",
      "required": true,
      "default": "Introducing data as a first‑class citizen on blockchain."
    },
    "description": {
      "type": "text",
      "required": true,
      "default": "A universal, cost efficient data layer where you control your data on Ethereum"
    },
    "primaryButtonText": {
      "type": "string",
      "required": true,
      "default": "Build now"
    },
    "secondaryButtonText": {
      "type": "string",
      "required": true,
      "default": "Read the litepaper"
    },
    "golemAnnouncement": {
      "type": "text",
      "required": true,
      "default": "GolemDB:now Arkiv\nRead the announcement"
    }
  }
}